function showLove() {
    let myName = prompt("Whats is your name?");
    const favColor = prompt("What is your favorite color?");
    if (favColor == "red") {
        window.alert("we are compatible!");
    }
    else if (favColor == "blue") {
        window.alert("we are compatible!");
    }
    else if (favColor == "black") {
        window.alert("we are compatible!");
    }
    else {
        window.alert("we are not compatible!");
    }
}
